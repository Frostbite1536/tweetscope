# Frontend (read-only demo) Vercel env
# Project: web-demo

# Must point to demo API project base URL (no /api suffix — routes include it)
VITE_API_URL=https://api-demo.example.com

# Optional if deploying under a sub-path. Usually leave empty on Vercel custom domain root.
BASE_NAME=
